<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <!-- Боковые отступы 8dp заданы на элементах (а не на корне), чтобы красная
         полоса «сейчас» могла идти во всю ширину виджета, от края до края. -->

    <!-- Шапка: сегодняшняя дата + количество задач слева, кнопки «+» и «Обновить» справа -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="10dp"
        android:paddingEnd="8dp"
        android:paddingBottom="4dp">

        <TextView
            android:id="@+id/widget_today"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#111827"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Число задач на сегодня — на светло-серой подложке -->
        <TextView
            android:id="@+id/widget_today_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:background="@drawable/count_pill"
            android:minWidth="24dp"
            android:gravity="center"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:textColor="#6B7280"
            android:textSize="14sp" />

        <!-- Распорка, чтобы кнопки ушли вправо -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/widget_add"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:padding="6dp"
            android:contentDescription="@string/add_task"
            android:src="@drawable/ic_add" />

        <ImageView
            android:id="@+id/widget_refresh"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="2dp"
            android:padding="6dp"
            android:contentDescription="@string/refresh"
            android:src="@drawable/ic_refresh" />

        <!-- Крутящийся индикатор во время обновления (на месте кнопки «Обновить») -->
        <ProgressBar
            android:id="@+id/widget_refreshing"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="2dp"
            android:padding="6dp"
            android:indeterminate="true"
            android:indeterminateTint="#5B5BD6"
            android:visibility="gone" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:scrollbars="none" />

        <TextView
            android:id="@+id/widget_empty"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/widget_empty"
            android:textColor="#9095A8"
            android:textSize="14sp" />
    </FrameLayout>
</LinearLayout>
